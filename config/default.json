{
  "max_workers_per_machine": 2,
  "profiles": {
    "formprep": {
      "yolo": false,
      "timeoutSec": 1200,
      "maxPerCwd": 1,
      "rules": "DEV only. Never SQL-flip UPD. Never AllUnprepared. Never commit secrets."
    },
    "teams": {
      "yolo": false,
      "timeoutSec": 600,
      "maxPerCwd": 1,
      "rules": "No AccessMedia.All. No claiming fixture speak is audible."
    },
    "mud": {
      "yolo": true,
      "timeoutSec": 600,
      "maxPerCwd": 2,
      "rules": "No spend. Stay on configured host."
    },
    "generic": {
      "yolo": false,
      "timeoutSec": 180,
      "maxPerCwd": 2,
      "rules": "No production deploys. No force-push."
    }
  }
}
