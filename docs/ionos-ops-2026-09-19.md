# IONOS ops log — 2026-09-19

Machine `ionos` (`WIN-MPRE8VI4U6U`). Repo `C:\ai\agentic_build`. This log is the session that unstuck Bob, started the fleet watcher, and packed the playbooks as skills.

## Commits (this thread)

| SHA | What |
|---|---|
| `21d9509` | `Test-PromptSecrets` refuses only `password=` / `XAI_API_KEY=` assignments. `Register-BobMachine` keeps drive-root `cwdRoots` as `C:\`. `Install-BobFleet` demand-starts the logon task and sets `ExecutionTimeLimit` 0. `Get-BobHealth` adds `watcher_up` / `last_seen`. `Watch-BobAgents.ps1` stall monitor. |
| `c95983d` | Stall script uses public health, not a private Fake-Grok helper. |
| `0561adc`–`9758dc6` | Grok Bot desktop liveness: do not exit the monitor on a stale marker pid. |
| `0a7ea9e` | Skills `unstick-grok-bot` + expanded `grok-build-fleet`. `GrokBotApi.py post` with token/url redact. Install copies every `.grok/skills/*/SKILL.md`. |
| `11c38ab` `1fdadf0` `e765c9c` | Monitor false positives: long job is not `watcher_down`; grok.exe already gone while watcher is still in the tick is not `running_orphan`; inbox behind a live running job is a queue, not `inbox_stale`. |
| `fe80fb1` | `profiles.generic.timeoutSec` 7200. Do not kill long product jobs at 180s. |
| `58ec2a1` `7ed5be6` | Bob spec/MRB loop skills; `box-usage`. |

## What we learned (ops)

- `BobFleet-ionos` registered after logon never fires until `Start-ScheduledTask` (or next logon). Result `267011` = never ran.
- Default task `ExecutionTimeLimit` 72h would kill the infinite poll loop.
- Named Grok Bot hangs live in Temporal (`GROK_BOT_SEND_STATUS_PENDING`). UI bubbles can be optimistic. Transcript is source of truth. Interrupt does not always end the turn. Desktop restart does not cancel Temporal. Cloud sandbox recreate (`preserveData` + `force`) does, then one short no-tool ping.
- `lastSeen` updates at tick start only. A live claimed `grok.exe` holds the loop.
- Sequential watcher: one running job, inbox waits.
- Human UI is a system tray icon (`Watch-BobTray.ps1`), not a blank PowerShell window. Flashes on ACTION_REQUIRED.

## Skills to load

- `grok-build-fleet` — queue, watcher, stall monitor tokens
- `unstick-grok-bot` — named-bot Temporal/sandbox hang
- `harvest-agent-skills` — hourly scan for new skills (see `tools/Install-SkillHarvest.ps1`)
