# Plan: TipForm polish
1. Verify Get-BobTrayAgentImage wired; fix dispose/visibility if icons still null.
2. Fix ToolTip on ? (ShowAlways / SetToolTip on help label; include model list text).
3. Read lesser/alternate Cursor pool from digest; show when main is 0.
4. Layout: overspend + cursor icon right-aligned on Cursor header row.
5. Uppercase machine names in tile render.
