# FR: TipForm usage dialog polish (systray)

## Summary
Simon (agentic_irc #186): usage dialog icons missing; ? does not show description/models; missing quota that still allows spend when any-model is 0%; move overspend next to Cursor logo on the right; machine names ALLCAPS.

## Acceptance
| ID | Criterion |
|----|-----------|
| A1 | Cursor + Grok section icons visible on TipForm. |
| A2 | `?` hover shows description / models list tooltip. |
| A3 | When primary Cursor pool is 0%, surface the alternate quota that still allows spend (if present in digest/box-usage). |
| A4 | Overspend line sits with Cursor logo on the right. |
| A5 | Machine name tiles render in ALL CAPS. |
